sonobuoy-config:
  driver: Job
  plugin-name: crossplane-conformance
  result-format: junit
spec:
  command: ["run.sh"]
  args:
  image: crossplane/conformance:v2.0.0-cf.1
  name: plugin
  resources: {}
  volumeMounts:
    - mountPath: /tmp/results
      name: results
